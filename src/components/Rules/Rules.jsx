import { NavLink } from "react-router-dom";

const Rules = () => {
  return (
    <div className="Rules">
      <div>Rules</div>
      <NavLink to="../start">Back</NavLink>
    </div>
  );
};

export default Rules;
